#
# sources: source_calculation
#
sources:
  identifier: <%= spaces.identifier %>
  virtual_space:
    width: <%= spaces.width %>
    depth: <%= spaces.depth %>
  reverberation_decay: <%= spaces.reverberation_decay %>
  source_positions: 
    1: [ <%= source.position.print_hirez %> ]
