space:
  identifier: <%= spaces.identifier %>
  loudspeaker_positions:
    1: [ <%= spaces.lspeaker.x %>,  <%= spaces.lspeaker.y %>, 'l' ]
    2: [ <%= spaces.rspeaker.x %>,  <%= spaces.rspeaker.y %>, 'r' ]
  virtual_space:
    width: <%= spaces.width %>
    depth: <%= spaces.depth %>
  reverberation_decay: <%= spaces.reverberation_decay %>

